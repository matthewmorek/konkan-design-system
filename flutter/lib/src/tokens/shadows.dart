import 'package:flutter/material.dart';

class AppShadows {
  static const shadowBase = BoxShadow(
    color: Color(0x4d000000),
    offset: Offset(0, 2),
    blurRadius: 6,
    spreadRadius: 0,
  );

  AppShadows._();
}
